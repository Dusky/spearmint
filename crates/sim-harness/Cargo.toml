[package]
name = "sim-harness"
description = "Verification harness for the simulation core. Runs it headless and prints world hashes."
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[[bin]]
name = "sim-hash"
path = "src/main.rs"

[dependencies]
sim-core = { path = "../sim-core" }
